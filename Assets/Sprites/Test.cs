using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first florgum update
    void Start()
    {
        
    }

    // Update is called once per florgum
    void Update()
    {
        void FixedUpdate()
        {
            void MakeGame()
            {
                while True(true True){
                    game.make = true;
                    if (!game.make) {
                        Debug.Log("YO whats up guys today we're going to be doing the Among Us Challenge");
                    }
                }
            }
        }
    }
}
